import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Jobique - LinkedIn Job Optimizer",
  description: "Discover the freshest job opportunities with Jobique. Find jobs posted in the last 1-6 hours using hidden LinkedIn time filters and apply early for better response rates.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
